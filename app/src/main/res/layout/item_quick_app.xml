<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="@dimen/padding_medium">

    <ImageView
        android:id="@+id/quick_app_icon"
        android:layout_width="@dimen/quick_app_icon_size"
        android:layout_height="@dimen/quick_app_icon_size"
        android:scaleType="centerInside" />

    <TextView
        android:id="@+id/quick_app_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/text_primary"
        android:textSize="@dimen/quick_app_text_size"
        android:layout_marginTop="@dimen/padding_small"
        android:maxLines="1"
        android:ellipsize="end"
        android:maxWidth="@dimen/quick_app_icon_size" />

</LinearLayout>